const chai = require('chai');
const assert = require('chai').assert;
const http = require('chai-http');

chai.use(http);

describe('basic server function', () => {
    it('server starts', done => {

    });
});